Clean up code.
Remove printfs.
change defines so they do not need to be rewritten everytime.
Play with different metrics.

Reduce message sizes: each node only sends data he changed and not the complete localC array
vectorization??
Strassen's matrix multiplication?

Not sure
	change char to int on file writing??
